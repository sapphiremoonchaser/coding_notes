# String Manipulation

### Extract a pattern/set of characters
`REGEXP_EXTRACT(column, '[0-9]+')`

### Replace a string with another string
`REGEXP_REPLACE(column, '[^a-zA-Z]', '')`

### Put strings together
`CONCAT(first, ' ', last)`

