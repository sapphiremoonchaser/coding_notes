## Creating datetime objects 
  
`dt = datetime(2025, 3, 10, 14, 30, 0)`  
  
## Current date and time
  
`now = datetime.now()` # local time
`utc_now = datetime.utcnow()` 
`today_date = date.today()` # Date, no time